spring:
#  data:
#    redis:
#      host: redis_boot
#      port: 6379
  datasource:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

springdoc:
  api-docs:
    path: /api-docs
#  swagger-ui:
#    defaultModelsExpandDepth: -1